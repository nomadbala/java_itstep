package Collection;

import Wildcard.Person;

import java.util.*;

public class Test {
    public static void main(String[] args) {
        List<Integer> intList = new ArrayList<>();
        intList = new LinkedList<>();
        intList = new Vector<>();
        Stack stack = new Stack();
        Set<Person> setAnimal = new HashSet<>();
    }
}
